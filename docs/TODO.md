# Actionable Tasks

## Immediate Tasks
- [ ] Implement advanced plugin configuration options and dynamically expose variation formulas via GUI components (Mutate/Adjust panels).
- [ ] Write integration tests to validate headless execution using a `test.flame` file payload.
- [ ] Expand code coverage on `JSTransform.java` to test Rhino JavaScript compilation edges.

## Pending Migration Tasks
- [ ] Refactor `Imager.java` to eliminate dependencies on `java.awt.Toolkit` when running with `--headless` flag.
