# Actionable Tasks

1. Address deprecated usages of `Toolkit.getMenuShortcutKeyMask()`, `InputEvent.ALT_MASK`, and `Class.newInstance()`.
2. Address `ScriptConverter` TODOs related to parsing elements.
3. Migrate `java.applet.Applet` to modern window management if needed.
