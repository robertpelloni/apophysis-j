# Dependencies

- Rhino: Used for JavaScript engine support inside Apophysis-j (`lib/rhino.jar`). Downloaded automatically via ant `pull` task.
- Thinlet: GUI library used for the interface. It's bundled within the `src/com/thinlet` folder.
